name: Deploy WordPress Theme

on:
  push:
    branches:
      - main  # Change this to your branch name

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up SSH key
      run: |
        mkdir -p /kunden/homepages/13/d829753805/htdocs/.ssh
        echo "${{ secrets.SSH_PRIVATE_KEY_BASE64 }}" | base64 -d > /kunden/homepages/13/d829753805/htdocs/.ssh/workflow
        chmod 600 /kunden/homepages/13/d829753805/htdocs/.ssh/workflow
        ssh-keyscan -t rsa ${{ secrets.SERVER_ADDRESS }} >> /kunden/homepages/13/d829753805/htdocs/.ssh/known_hosts


    - name: Deploy Theme
      run: |
        rsync -azP --delete-after --exclude='.git*' --exclude='.github*' . ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_ADDRESS }}:"/2 - www/dev/WP_blank.5pectre.com/wp-content/themes"

    - name: Clear SSH key
      run: rm -f ~/.ssh/workflow
